import { ReactNode } from "react";
import { DataProvider, useData } from "./data/index";

// Re-exporting the DataProvider and useData hook
export { DataProvider, useData };

// This file remains for backwards compatibility
// It simply re-exports the refactored context and hook
